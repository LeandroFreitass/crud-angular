import { HorasTrabalhadasPipe } from './horas-trabalhadas.pipe';

describe('HorasTrabalhadasPipe', () => {
  it('create an instance', () => {
    const pipe = new HorasTrabalhadasPipe();
    expect(pipe).toBeTruthy();
  });
});
